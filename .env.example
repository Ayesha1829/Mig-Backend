PORT=3002
NODE_ENV=development
# FRONTEND_URL is a single frontend origin (e.g. https://mig-frontend.vercel.app)
FRONTEND_URL=https://mig-frontend.vercel.app

# ALLOWED_ORIGINS can be a comma-separated list of allowed origins for CORS
# If set, it takes precedence over FRONTEND_URL. Example:
# ALLOWED_ORIGINS=https://mig-frontend.vercel.app,http://localhost:3001
ALLOWED_ORIGINS=

# JWT secret used to sign tokens (override in production)
JWT_SECRET=your_jwt_secret_here

# Admin password for admin endpoints (override in production)
ADMIN_PASSWORD=changeme_admin_password
